import { DirectivereDirective } from './directivere.directive';

describe('DirectivereDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivereDirective();
    expect(directive).toBeTruthy();
  });
});
